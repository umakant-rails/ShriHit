<div class="row">
  <div class="col-md-12">
    <div class="card">
      <div class="card-body h-400">
        <div class="page-subject-header">
          <div class="col-md-12 text-start article-title text-primary">
            संत जीवन वृतांत सूची
          </div>
        </div>
        <div class="dropdown-divider"></div>
        <table class="table">
          <thead class="table-primary ps-0 pe-0">
            <tr>
              <th>#</th>
              <th>शीर्षक</th>
              <th>संत का नाम</th>
              <th>गतिविधि</th>
            </tr>
          </thead>
          <tbody>
            <% if @saint_bio_events.present? %>
              <% @saint_bio_events.each_with_index do |saint_bio_event, index| %>
                <tr>
                  <td><%=index+1%></td>
                  <td class="text-primary1">
                    <%= link_to saint_bio_event.title, saint_bio_event_path(saint_bio_event) %>
                  </td>
                  <td class="text-primary1">
                    <%= link_to saint_bio_event.author.name, author_path(saint_bio_event.author_id) %>
                  </td>
                  <td>
        						<div class="row">
        							<div class="col-md-2">
        								<%= link_to saint_bio_event do %>
        								  <i class="fa fa-eye"></i>
        								<% end %>
        							</div>
        							<div class="col-md-2">
        								<%= link_to edit_saint_bio_event_path(saint_bio_event) do %>
        								  <i class="fa fa-edit"></i>
        								<% end %>
        							</div>
        							<div class="col-md-2">
        								<%= form_for(saint_bio_event, html: { method: :delete, onSubmit: "return confirm('Are you sure you wish to delete?');" }) do |f| %>
        						      <button type="submit" class="btn" style="padding: 0;margin-bottom: 1px;">
        						        <i class="fa fa-trash" style="color:red;"></i>
        						      </button>
        						    <% end %>
        						  </div>
        				    </div>
        					</td>
                </tr>
                <% index = index + 1%>
              <% end %>
            <% else %>
              <td colspan="4">
                अभी रिकॉर्ड उपलब्ध नहीं है.
              </td>
            <% end %>
          </tbody>
        </table>
        <%= link_to "संत जीवन वृतांत जोड़े", new_saint_bio_event_path, class: "mt-2" %>
      </div>
    </div>
  </div>
</div>
