var options = '<option value="">सेक्शन को चुनिये</option>';

<% @sections && @sections.each do | section | %>
  options = options + '<option value="<%= section.id %>"><%= section.name %></option>'
<% end %>
$("#chapter_parent_id").html(options);